## 1. Shared harness scaffolding

- [x] 1.1 Create the calibration harness module location (e.g. `src/solver/cascadeCalibration.*`), reusing `cachePerformance.test.ts`'s seeded `mulberry32` PRNG and `SolverBoard` snapshot helper rather than reimplementing them. Verify by importing both and generating a reproducible mine layout for a known seed twice, asserting identical output.
- [x] 1.2 Add a small shared bias-reporting utility that computes, per group of `(predicted, actual)` pairs: raw mean bias (`mean(predicted - actual)`), and geometric-mean ratio bias (`exp(mean(log(predicted/actual)))`), per design.md Decision 2. Verify with a unit test on a hand-picked set of `(predicted, actual)` pairs against manually computed expected values, including a case that would visibly differ between arithmetic-mean-of-ratio and geometric-mean-of-ratio (to confirm the correct one is implemented).

## 2. Part 1 — isolated estimator calibration

- [x] 2.1 Implement bounded-region generation for each boundary-distance bucket (`interior`, `edge-adjacent`, `corner-adjacent`), sized relative to `cascade-aware-eig`'s existing `epsilon`/max-node-count pruning per design.md Risks. Verify with a test asserting the generated region is large enough to contain the BFS's full pruned reach for a representative `(q, d)` input (no truncation-at-region-edge artifacts).
- [x] 2.2 Implement exact combinatorial ground-truth entropy computation for a concrete, small bounded region given a mine placement and a root cell forced to be 0. Verify with a unit test against a hand-computable small fixture (e.g. a 3x3 or 5x5 region with a known mine count) where the exact entropy drop can be independently verified by direct enumeration.
- [x] 2.3 Implement the Part 1 Monte Carlo loop: for each of the three preset densities (`mineCount/(width*height)` from `src/game/difficulty.ts`, per design.md Decision 2) x each boundary bucket, draw many concrete mine placements at that density, compute `predicted = expectedCascadeInfo(x)` by calling `frontierSolver.ts`'s real exported logic (not reimplemented, per design.md Decision 4) and `actual` via task 2.2, and aggregate bias via task 1.2's utility. Verify by running the loop end-to-end for one (density, bucket) combination and confirming it produces a finite, non-NaN raw-bias and ratio-bias pair.
- [x] 2.4 Run Part 1 across all 3 densities x 3 boundary buckets (9 cells) and produce a results table/report. Verify by confirming all 9 cells report values and that raw bias is positive (overestimate direction) in at least the interior bucket, consistent with the theoretical prediction this change exists to check — and treat any non-positive-bias interior result as a finding to report, not a bug to silently fix.

## 3. Part 2 — fusion-blind-spot tracking over a real game

- [x] 3.1 Implement frontier-component-set extraction from a `solve()` result (or reuse if already exposed), sufficient to determine which unrevealed cells belong to which connected frontier component before and after a reveal. Verify with a unit test on a fixture board with two known-separate components, asserting they're reported as distinct sets.
- [x] 3.2 Implement fusion classification: given pre-reveal and post-reveal component sets, determine whether cells from two or more pre-reveal components are joined into one post-reveal component. Verify with unit tests covering a no-fusion move (cascade stays within one component) and a fusion move (constructed fixture where a cascade's border cell bridges two components).
- [x] 3.3 Implement the solver-guided playthrough driver: for a seeded random board at each preset, repeatedly select the unrevealed cell with the highest `frontierResult.eig`, reveal it via the real `Board`/`floodReveal` path, and at each step call `computeRevealFeedback` (from `src/game/revealFeedback.ts`, unmodified) to get `predictedEig`/`revealedInformation`, plus task 3.2's fusion classification. Verify by running one full playthrough on a small preset (Beginner) to completion (or a fixed move budget) without error, and asserting each recorded move has non-null `predictedEig`, a finite `revealedInformation`, and a boolean `fusion` flag.
- [x] 3.4 Aggregate Part 2 results across `revealedFraction` buckets (e.g. deciles), split by `fusion: true`/`false`, reporting raw and ratio bias per design.md Decision 2/3. Verify by running the full driver across all 3 presets and confirming the aggregation produces non-empty results for both fusion groups (or explicitly reports zero fusion events observed, if that's what happens, rather than silently omitting the group).

## 4. Reporting

- [x] 4.1 Produce a combined, human-readable summary (console output or a written report file) covering both parts' results: Part 1's 3x3 bias table, and Part 2's bias-over-revealed-fraction curves split by fusion. Verify the harness can be run standalone (e.g. `npm run` script or direct `vitest`/node invocation) and produces this summary without manual post-processing.
- [x] 4.2 Cross-check: compare Part 1's interior-bucket, low-revealed-fraction Part 2 numbers against each other as a sanity check (they should be measuring similar conditions via different methods and should roughly agree in direction and rough magnitude). Verify by running both parts and confirming this comparison is included in the task 4.1 summary, flagging (not silently resolving) any large disagreement as a finding.
