## 1. Baseline and safety net (design D6)

- [x] 1.1 Add a `subsetKeyCallCount` test-only probe (`getSubsetKeyCallCountForTest`/`resetSubsetKeyCallCountForTest`) alongside the existing `growTrimCallCount`/`enumerationCallCount` probes, incremented on every subset key build (hits included), and verify with a unit test that two `resolveSubset` queries for the same subset increment it twice while `growTrimCallCount` increments once.
- [x] 1.2 Add an output-snapshot helper to `src/solver/bottleneckProfile.test.ts` (or a sibling gated test) that serializes `computeExplanations`' full output - every certain cell's `clueCells` and `premiseCells` - across a fixed set of harness boards to a file. This is the tripwire for design D6's equivalence arguments, not the acceptance gate; verify it produces a stable file across two consecutive runs.
- [x] 1.3 Capture the pre-change baseline in one session on one machine: run `PROFILE=1 npx vitest run src/solver/bottleneckProfile.test.ts --reporter=verbose`, record per-difficulty `solve`/`explain` totals and the `subsetKeyCallCount` per-move figure, and write the snapshot from 1.2 to disk. Verify the recorded baseline table has all three difficulties populated.
- [x] 1.4 Add a test asserting that BFS layers computed over a component's `relevantConstraints` are identical to those computed over the whole board's `constraints`, for every certain cell on a battery of boards including a multi-component board - this checks design D1's claim that frontier components are the connected components of the clue graph rather than assuming it.
- [x] 1.5 Add the spec-property tests that are this change's actual acceptance gate, run over a battery of boards including a multi-component board: for every certain cell's explanation, assert it is **sufficient** (its clue set alone forces the cell to its reported value), **irreducible** (removing any single clue loses that forcing), and **component-confined** (every clue and premise cell belongs to that cell's own frontier component). Verify they pass against the current, unmodified implementation before any stage lands, so a later failure indicts the stage rather than the test. Determinism is already covered by `frontierSolver.test.ts:428` and `:531`; confirm those cover `computeExplanations` and extend them if they only cover a narrower entry point.

## 2. Stage 1 - per-component index and on-demand adjacency (design D1, D2 - unconditional)

- [x] 2.1 Introduce the `ComponentIndex` type and a builder that constructs `constraints` (the existing `relevantConstraints`), `cluesByCell`, `constraintByKey`, `idByClueKey`, and `flagGivens` once inside `computeExplanations`' component loop; verify with a unit test that `cluesByCell` maps each unrevealed cell to exactly the clues referencing it on a hand-checked board.
- [x] 2.2 Replace `buildClueAdjacency` with an on-demand `neighboursOf(index, clue)` derived from `cluesByCell`, delete `buildClueAdjacency`, and verify `computeClueBfsLayers`' existing layer tests (`frontierSolver.test.ts:337,350`) pass unmodified.
- [x] 2.3 Change `computeExplanationForCell` (and `growSufficientSet`/`quickXplain`/`extractPremiseKeys` beneath it) to take the `ComponentIndex` in place of loose `constraints`/`flagGivens`/cache arguments, so the component's clues - not the whole board's - flow through the pipeline; verify the full `frontierSolver.test.ts` suite passes.
- [x] 2.4 Update the test-only exports (`computeCellExplanationForTest`, `quickXplainForTest`, `resolveSubsetForTest`, `createSubsetVerdictCacheForTest`) to the index-based shape, carrying over every existing assertion; verify each of their current tests still passes in its new form.
- [x] 2.5 Run `npm test` and verify the 1.5 spec-property tests pass. Then diff the 1.2 output snapshot against the 1.3 baseline: a difference (or an explainer fixture that moves) is not an automatic failure but must be diagnosed before proceeding - either an equivalence argument in design D1/D2 is wrong, or it is a genuine tie between equally-minimal explanations, in which case record which and update the fixture.
- [x] 2.6 Re-run the profiler and record the stage-1 row (per-difficulty `solve`/`explain` totals); verify the `solve` column is flat versus baseline, and repeat the run if it is not.

## 3. Stage 2 - lazy BFS layers (design D3 - unconditional)

- [x] 3.1 Replace `computeBfsLayers` with the `walkClueLayers` generator from design D3, preserving the seeding rule, the same-distance-one-layer rule, and the per-layer `sort()` verbatim; verify a unit test that draining the generator yields exactly the array `computeBfsLayers` returned before.
- [x] 3.2 Change `growSufficientSet` to iterate the generator and return as soon as `resolvesTo` holds, and reimplement `computeClueBfsLayers` as a full drain; verify with a test that a cell resolved by its first layer causes the generator to advance no further than one layer (e.g. by counting yields via a wrapping iterator in the test).
- [x] 3.3 Run `npm test`, verify the 1.5 spec-property tests pass, and diff the output snapshot against the baseline, diagnosing any difference per task 2.5 rather than treating it as a pass or a failure on its own.
- [x] 3.4 Re-run the profiler and record the stage-2 row; verify the `solve` column is flat.
- [x] 3.5 Evaluate design D6's stated falsifier against the stage-2 row: if Beginner's per-move explain time has not fallen substantially, record the finding in design.md's attribution table and re-diagnose with the user before starting section 4. Sections 2 and 3 stand either way - they carry design D1's component-scoping invariant and are not reverted on performance grounds; verify this task by confirming the falsifier verdict is written down, whichever way it went.

## 4. Stage 3 - component-scoped cache and id-based subset keys (design D5, D4 - contingent on section 3's measurements)

- [x] 4.1 Move `SubsetVerdictCache` from a `computeExplanations`-scoped local onto `ComponentIndex`, so each component gets its own; verify the existing cross-cell-reuse test still shows a shared cache doing less work than per-cell caches, and that the `ComponentCache` hit/miss assertions still pass unmodified.
- [x] 4.2 Add `givensPrefix` to `ComponentIndex` (the component's `flagGivens` serialized once) and replace `subsetSignature` with `subsetKey(index, constraints)` built from sorted integer ids plus that prefix; verify the carried-over key tests - order independence, and different givens producing a different key - both pass.
- [x] 4.3 Add a test asserting that no two distinct clue subsets of a component produce the same `subsetKey`, over all subsets of a small hand-built component, covering design D4's dropped content-hashing safety net.
- [x] 4.4 Run `npm test`, verify the 1.5 spec-property tests pass, and diff the output snapshot against the baseline, diagnosing any difference per task 2.5 rather than treating it as a pass or a failure on its own.
- [x] 4.5 Re-run the profiler and record the stage-3 row; verify the `solve` column is flat.

## 5. Record and correct

- [x] 5.1 Append the per-stage attribution table (baseline plus one row per stage, per-difficulty `solve`/`explain` totals and explain-per-move, with the `subsetKeyCallCount` figures) to this change's `design.md`, stating for each stage whether it paid and by how much; verify every stage landed in sections 2-4 has a row.
- [x] 5.2 Append a dated addendum to `openspec/changes/optimize-explanation-extraction/design.md`'s Measured Results section correcting its "remaining gap is dominated by the uncapped exponential enumeration" conclusion, citing the 37%-calls/7%-time divergence and the Beginner row, and pointing at this change; verify the original table, reasoning, and conclusion text are left in place above the addendum.
- [x] 5.3 Confirm the still-outstanding enumeration-cap follow-up is described accurately in both documents as owning the multi-second Expert hangs (the moves the harness skips as dangerous), distinct from the setup overhead this change addresses.
